<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.koinwarga.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
            android:name=".App"
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.send.SendActivity"></activity>
        <activity android:name=".ui.pay_preview.PayPreviewActivity"></activity>
        <activity android:name=".ui.pay_scanner.PayScannerActivity"></activity>
        <activity
                android:name=".ui.manage_account.ManageAccountActivity"
                android:launchMode="singleTop"></activity>
        <activity
                android:name=".ui.main.MainActivity"
                android:label="@string/title_activity_main"
                android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".ui.scanner.ScannerActivity"></activity>
        <activity android:name=".ui.landing.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>