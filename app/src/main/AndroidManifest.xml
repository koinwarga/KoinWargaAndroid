<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        package="com.koinwarga.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
            android:name=".App"
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.registering_people.RegisteringPeopleActivity"></activity>
        <activity android:name=".ui.send.SendActivity" />
        <activity android:name=".ui.pay_preview.PayPreviewActivity" />
        <activity android:name=".ui.pay_scanner.PayScannerActivity" />
        <activity
                android:name=".ui.manage_account.ManageAccountActivity"
                android:launchMode="singleTop" />
        <activity
                android:name=".ui.main.MainActivity"
                android:label="@string/title_activity_main"
                android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.scanner.ScannerActivity" />
        <activity android:name=".ui.landing.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>